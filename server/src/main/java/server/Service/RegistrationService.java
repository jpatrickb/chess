package server.Service;

import model.User;

public class RegistrationService {
    public static User registerUser(User user) {
//        Todo: actually build the function
        return new User(null, null, null);
    }
}
